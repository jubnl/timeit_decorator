name: Upload Python Package

on:
  release:
    types: [ published ]

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 2

#      - name: Set up Python
#        uses: actions/setup-python@v6
#        with:
#          python-version: '3.13'

      - name: Install build and project dependencies
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip3 install build
          pip3 install .
          pip3 install .[dev]

      - name: Run tests with pytest
        run: |
          source .venv/bin/activate
          pytest --cov --cov-branch --cov-report=xml --junitxml=junit.xml -o junit_family=legacy

      - name: SonarQube Analysis
        run: |
          source .venv/bin/activate
          pysonar \
            --sonar-host-url ${{ vars.SONAR_HOST_URL }} \
            --sonar-project-key ${{ vars.SONAR_PROJECT_KEY }} \
            --sonar-sources ${{ vars.SONAR_SOURCES }} \
            --sonar-tests ${{ vars.SONAR_TESTS }} \
            --sonar-python-version ${{ vars.SONAR_PYTHON_VERSION }} \
            --sonar-python-coverage-report-paths coverage.xml \
            --token ${{ secrets.SONAR_TOKEN }}

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: junit.xml

      - name: Build package
        run: |
          source .venv/bin/activate
          .venv/bin/python -m build

      - name: Publish package
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          attestations: true